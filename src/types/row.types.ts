export type Row = {
  state: string;
  city: string;
  installs: string;
  trials: string;
  date: string;
  conversion?: string;
};

export type ChartData = {
  installs: string;
  trials: string;
  date: string;
};
